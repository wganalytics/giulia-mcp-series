# PRJ-05 secure_server — copie para .env e preencha.
# A chave é GERADA pelo próprio sistema (não é segredo de terceiros):
#   uv run python src/main.py create-user --name "Voce" --email voce@ex.com --password ***
#   uv run python src/main.py gen-key --user-id 1
# Cole a "full key" retornada aqui:
GIULIA_AI_API_KEY=
