[project]
name = "prj-01-cap02-ping-server"
version = "0.1.0"
description = "Servidor MCP mínimo: ping/echo + health-check TCP real"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.28.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
